<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ site.description }}">
    <meta name="author" content="{{ site.author }}">
    <meta name="page-topic" content="{{ pageTopic | default(site.defaultPageTopic | default('Kunst, Malerei, Atelier')) }}">
    <title>{% if title %}{{ title }} | {% endif %}{{ site.name }}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="stylesheet" href="{{ '/css/styles.css' | url }}">
    <link rel="stylesheet" href="{{ '/vendor/glightbox/glightbox.min.css' | url }}">
</head>
<body>
 
    {% include "top_header.njk" %}
    {% include "nav.njk" %}
    <main>
        {{ content | safe }}
    </main>
    {% include "footer.njk" %}
    <script src="{{ '/vendor/glightbox/glightbox.min.js' | url }}"></script>
    <script src="{{ '/js/main.js' | url }}"></script>
</body>
</html>
